#include<stdio.h>
int a[],int size,e;
void array (int a[],int size)
{
for (int i=0;i<size;i++)
{
    scanf("%d",&a[i]);
    printf("%d",a[i]);
}
void sequence(int a[i],int size)
{
    for(int i=0;i<size;i++)
    {
        if(a[i]==1&a[i+1]==2&a[i+2]==3)
        {
            printf("present");
        }
    }
}
int main()
{
    scanf("%d",&size);
    array(a,size);
    sequence(a,size);
}
{
    printf("%d",str)
}
}
